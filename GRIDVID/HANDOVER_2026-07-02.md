---
title: "GRIDVID HANDOVER — error-slice mined + P2 band-projection deep rule shipped"
date: 2026-07-02
read_first: yes
continues: HANDOVER_2026-07-01.md
---

# HANDOVER — read this first (then the 2026-07-01 one for the World-Model framing)

Continuing GRIDVID, the program-first synthetic ARC-AGI-2 generator in `archer`. The objective is unchanged
(see 2026-07-01 §1): the DSL is the LANGUAGE a non-agentic consumer LLM GENERATES programs in (INDUCTION);
corpus = (task → DSL-program) labelled pairs; engine verifies. Operating rules unchanged (REGOLA 0: study the
real system, never build a parallel toy; HTML-rule: show every change; commits Mario-only; self-tests green
before push).

## 1. What I did this session — the DECISIVE experiment first (red-team #1)
Before adding rules blindly I ran the error-slice the red-team calls the #1 decisive experiment: **mined the
expressivity annotations already in `DATASET/descriptions/{training,iq}/*.md`** (275 described tasks). Result:

- `expressible_in_dsl`: **yes 154 / partial 108 / no 13** → ~44% of real tasks are NOT fully expressible today.
- AGI-2 hardness axes present: compositional 151, contextual 117, symbolic 84 (of 275).
- transform_depth histogram: 1→27, 2→105, 3→103, 4→23 (the mass is at depth 2–3 — matches our Dchain target).
- **111 concrete `EXPRESSIVITY-GAP:` lines**, clustered by missing primitive:
  1. **path/line/ray — 20** (conditional/dotted/staircase rays, band-projection, retract, gap-fill) ← top gap
  2. **select/relational — 19** (landmark correspondence/alignment, nearest, same-silhouette-different-scale)
  3. **map/legend/symbol — 17** (glyph→value dict, multi-panel legend) ← PARTLY covered by P1 legend
  4. **shape/construct — 13** (stamp glyph along marker step, decompose blob)
  5. **count/arith — 11**; topology/enclosure — 9 (PARTLY covered by P4 containment); grid-partition — 6.

Takeaway: the two BIGGEST *unaddressed* gaps are **path/ray** and **relational-correspondence**. This both
confirms the 2026-07-01 roadmap order (P2 band-projection was next) AND tells us P3 correspondence is the right
follow-up (not P5 count). Reproduce the slice with the aggregation one-liners in the session log, or re-run over
`DATASET/descriptions/` any time new descriptions land.

## 2. What SHIPPED (archer, program.js; both self-tests GREEN; HTML shown)
**P2 band-projection** — the 2nd grid-partition/projection deep rule, correct-by-construction, program-first,
in the SAME derive→consume style as P1 legend / P4 containment:
- **applyNode**: `derive_bands` (partition rows by full-width separator lines → per-band map, keyed by each band's
  left-edge KEY swatch) → `apply_bands` (recolour every neutral object to its band's key colour; separators/keys
  untouched). Both partition AND per-band symbol-grounding are read off the grid and vary per example.
- Wired through `intrinsicMetrics` (Dchain 2, S 2, score 0.54), `nlNode`+`dslText` (NL≡DSL:
  `seq(derive(B=bands) ▷ apply_bands(B))`), `focusOf` (derive step white-highlights the separators+keys — 14 cells
  — the human "pointing"), a self-test assertion, `BAND_PROG` + `sampleBandScene` + `generateBands`, CLI `--bands`,
  module export. Difficulty 0.54, on par with containment.
- Command: `node program.js --bands N -o out/bands.jsonl`; render `build_trace_showcase.js` / `build_corpus_showcase.js`.

## 3. The deep-rule roster now (each = one intrinsically-deep rule, Dchain≥2, white-selection trace)
- **P1 legend** (`--legend`): symbol-grounding colour map. Dchain 3.
- **P4 containment** (`--contain`): topology inside/outside. Dchain 2.
- **P2 band-projection** (`--bands`): grid-partition + per-band key. Dchain 2.  ← NEW

## 4. REMAINING roadmap (do next, program-first, in the real system)
1. **P3 correspondence / relational alignment** (the #2 gap, 19 lines) — `derive_correspondence`→`apply_by_target`:
   per-target solved transform / landmark alignment (e.g. align a template to each target by a matched anchor, then
   apply the template's per-cell rule). This is the next highest-leverage deep rule. Design it correct-by-construction
   (place a template + N targets whose match is unambiguous), Dchain≥2, white-highlight the matched anchor.
2. **Richer P2 (true ray)** if cheap: a source marker shooting a ray whose SEGMENTS recolour per band crossed — the
   literal "band-conditioned ray" of eval `05a7bcf2`. Current P2 is band-recolour (region projection); the ray
   variant closes the dotted/staircase sub-gaps too.
3. **Program-first corpus assembler** over {legend, containment, band, composed-dependent, dispatch} stratified on
   Dchain/family with the sensibility gate; LLM only for taste (2026-07-01 §6 still stands).
4. **Port dormant engine constraint ops** (`solveLatin`/`markEnclosedRegions`) as combinators (Latin-square/symmetry-repair).

## 4b. GOD BUILDER shipped (same session, Mario's direct ask)
Mario: "each function should have a paired difficulty; organization hierarchical in families; a function admits
only some other functions by construction; enrich the families — like building a video game."
Shipped **`builder.js`** — the live proposer (`cli.js proposeMenu`) now delegates to it:
- **14 hierarchical families** (`FAMILY_DB`, domain/family) with videogame ROLES: `base` (the ONE core mechanic,
  exactly one per menu, always first), `modifier` (keys/conditions the base — NOT a second rule), `finisher`
  (one whole-grid op, always LAST). Static and video domains NEVER mix.
- **admits-graph by construction**: a base family admits only its listed families (unified from the PAN-176
  super_suggester typed prototype, which was parallel and never wired in); a family repeats only if it admits
  itself (physics+physics). Nothing outside the graph is ever offered together.
- **Paired difficulty everywhere**: template's own `difficulty` else its family band midpoint (fixed the 18
  templates with `?`); composed menu difficulty is additive with a reported per-function breakdown (`budget`),
  not `max()`; `--target d` sets the budget on `generate-llm`.
- **Role-aware prompt** (`menuToPrompt`): CORE MECHANIC / REFINEMENT / FINISHER / difficulty target.
- Menu shape backwards-compatible ({functions, augmentations, composeHint, difficulty} + {target, budget,
  role/family/difficulty per fn}); all pre-existing cli self-test checks still green + 4 new god-builder checks;
  `node builder.js --self-test` (taxonomy integrity, 120-seed structural sweep, determinism, k-honouring).
- Showcase: `node build_builder_showcase.js -o out/builder_showcase.html` (taxonomy + admits + budgeted menu
  sweep with real grids). NEXT here: per-family curriculum sampling for the corpus assembler; feed `admits`
  into program.js composed-chain sampling; difficulty calibration against solver pass-rates.

## 4c. Same session, later rounds (PAN-195 ✅ / PAN-197 ✅ / PAN-196 started)
- **PAN-195 cleanup DONE** (checkpoint `a49852e` → `5e1ae5b`): 93 → 39 .js files (36 dead one-offs deleted, all
  git-recoverable at the checkpoint); ONE showcase entry `showcase.js corpus|trace|builder|construct|graph`;
  old handovers → `_archive/`; README refreshed. KEPT deliberately after survey: gen2–5/gen_deep/gen_iq/gen_logic/
  reasoning (live gridgen family modules), solver.js (live dep), skins2.js (render+classify layer), compose_deep.js
  (solver self-test negative-control), build.js (studio inliner). Repo map HTML shown.
- **PAN-197 graph explorer DONE** (`0d608eb`): `build_graph_explorer.js` / `showcase.js graph` — interactive
  family/admits graph (clickable nodes/edges, per-target menu sampler over 149 precomputed REAL buildMenu results).
- **PAN-196 STARTED**: `program.js --corpus N` = the stratified program-first corpus assembler
  (legend 18% / containment 16% / bands 16% / composed_d2 18% / composed_d3 16% / contextual 16%), deterministic
  interleave, honest per-stratum coverage report (⚠UNDER flags). REMAINING for PAN-196: solver-calibrated
  difficulty, admits-graph inside sampleChain, PAN-176 slot unification into builder.js, sub-families,
  corpus-level family coverage metrics.

## 4d. Longer thinking + 8 new hard templates + HAIKU-as-Qwen run (Mario's asks, same day)
- **Longer thinking**: `LEGEND_CONTAIN_PROG` = legend→containment CHAINED deep rule (Dchain 5, score 0.88,
  **6-grid thinking trace**); corpus strata rebalanced (deep_seq 16% + composed_d4 10%); trace-length histogram
  moved from {2..4} to {2:17, 3:24, 4:18, **6:11**} on corpus_v2.
- **8 new engine-verified hard templates** (4 seeds each, all gates): extract_zoom_largest (3-step chain),
  odd_symmetry_extract, figure_sub, occluder_reveal, overlay_key_overlap (in-scene key = symbol grounding),
  parity_dispatch (grounded key swatches), sortrows_bars, filter_then_arrange (filter→order). Library 62→70.
  LEARNED (idiom): multi-verb chains and whole-grid verbs must NOT end with `snap 1` — those verbs push their own
  frame and a trailing snap re-renders the un-transformed bodies (this made sort_rows look like a no-op).
- **HAIKU-as-Qwen experiment** (real pipeline: god-builder k=3 target 0.85 menus → `cli.js prompt` → Haiku
  subagents → extractScene → full gates → 1 feedback retry): **2/6 first-shot, 4/6 after self-correction**.
  Reject taxonomy: invented syntax ×2 (`where shape is not`, `copy it` outside dispatch), magic colours ×2;
  on retry the fixes often introduce NEW errors (invented shape `circle` despite the explicit guardrail);
  one "fix" = verbatim exemplar copy (novelty escape hatch). Confirms: self-correcting loop WORKS mechanically,
  weak-model ceiling is real, k=3 composition is where they break. Artifacts: `out/haiku_hard_k3.{jsonl,html}`,
  prompts+replies in the session scratchpad.

## 5. Tracking
Linear **PAN** (ARC-AGI Series) — this session = error-slice + P2 band-projection. Scintilla
`arc-agi-2-english-knowledge-base` node under GRIDVID hub. Flywheel GRIDVID hub `ad86eacd-0b93-504b-b714-aec335dcc778`.
Note: the working tree carried pre-existing uncommitted changes from the prior session (TODO.md, gen_hard.js,
gif.js, index.html, some scenes/library/*.txt, untracked build_*.js) — I did NOT touch or commit those; my commit
is program.js + this handover only.
