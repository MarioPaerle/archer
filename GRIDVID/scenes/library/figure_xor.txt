# RULE: XOR the two figures (cells in exactly ONE of them survive; the shared cells cancel).
# Carpenter figure-subtraction / PGM XOR. Both figures are top-left aligned, then combined.
rule keep the cells that belong to exactly one of the two figures (XOR); shared cells cancel
concept boolean figure-algebra xor combine
difficulty 0.7
grid rand 12 15 10 12
seed 1
spawn square rand 3 5 at 1 1 color rand 2 8 id A
spawn diamond rand 2 3 at 1 7 color rand 2 8 id B
hold 1
cut
combine A B op xor into 1 1 color 4
snap 1
vary flip rot
