# TASK: count the cells, output a bar of that length (literal cardinality).
rule output a horizontal bar whose length equals the number of coloured cells
concept counting cardinality
vary flip zoom color
grid 12 8
bg 0
seed 1
scatter rand 2 7 color 2 box 0 1 3 10   # a RANDOM number of cells → the count varies per example
hold 1                                   # IN: the scattered cells
cut
tally color 2 at 6 1 color 6             # count them, draw a bar that long
remove color 2                           # OUT: just the bar (the count)
snap 1
